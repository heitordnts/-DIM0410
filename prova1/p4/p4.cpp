#include <iostream>
#include <set>

using namespace std;

int main(){
	/*
	forma de guarda os dados:
		guardar cada conjunto de problemas de cada amigo, como um 'set'.
		para cada elemento de cada set procuramos se ele existe nos outros dois
		se existir em algum ,ele nao sera acrescentadoa lista do amigo correspondente
		verificamos qual a maior lista de problemas only e imprimimos quem e quais
	*/
	int n;	
	cin >> n;

	return 0;
}
